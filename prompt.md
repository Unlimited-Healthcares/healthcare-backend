We need a complete and developer-friendly documentation of all appointment-related endpoints so the frontend team can properly integrate the “Make Appointment” and related features.

Please list all endpoints under the /appointments module, including:

HTTP methods (GET, POST, PATCH, DELETE, etc.)

Endpoint paths

Description (what the endpoint does)

Required request body (DTOs) and parameters

Example request and response (with real or mock data)

Authentication requirements (if any)

Possible error responses (status codes + error messages)